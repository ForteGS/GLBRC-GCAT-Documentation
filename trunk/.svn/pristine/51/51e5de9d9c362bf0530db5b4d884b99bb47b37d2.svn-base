\name{model}
\alias{model}
\title{function model}
\description{
  Function to create a new model
}
\usage{
  model(name, expression, formula, guess)
}
\arguments{
  \item{name}{
    A simple description of the model.
  }
  \item{expression}{
    An object of class "expression" that evaluates the response (transformed OD) with respect to the variable Time.  
  }
  \item{formula}{
    same as expression, but with y as the response
  }
  \item{guess}{
    A function that computes initial guesses for the parameters given a well object with a valid "screen.data" slot containing useable OD values and slope estimates.
  }
}

